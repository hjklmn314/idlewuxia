# QA-UI-001 / T05-01 Manual Visual Acceptance — 2026-07-25

## Verdict

`PASS` for the active UI surface and the configured `tmnpc01d` modal path.
`UI_EarlyCombat` remains explicitly postponed by `COMBAT-002`.

## Human inspection

I inspected the six conditional screenshots generated by the real Edge runs:

- `UI_NpcInteraction`: 360x800, 390x844, 412x915.
- `UI_ChapterLoop`: 360x800, 390x844, 412x915.

I also inspected the three `tmchoice01` modal screenshots at the same
viewports. The NPC interaction buttons remain inside the portrait shell; the
conditional feedback panel is readable; the chapter-loop map surface has no
horizontal overflow; and the modal title/options remain inside the viewport
with the background inert. At 360x800 the longer NPC feedback wraps to two
lines without clipping; at 390x844 and 412x915 it remains readable with the
same hierarchy. No console error or warning was present in the corresponding
reports.

## Evidence boundary

The screenshots and DOM/state reports are generated under the ignored
`outputs/wuxia_visual_matrix/20260725_qa_ui_001_tmnpc01d_final` directory. They are
local acceptance evidence and are intentionally not committed. The tracked
route contract, runner, test, and this record are the reproducible source of
the gate.

This is not a commercial release approval: final APK signing, real-device
lifecycle, performance, asset ownership, store metadata, rollout, and
rollback remain open.
