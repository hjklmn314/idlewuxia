# ARCH-001 Slice 5 人工视觉与交互验收

任务：`ARCH-001 Slice 5 — ChapterSession`

日期：2026-07-22

判定：`PASS WITH KNOWN LIMITATIONS`

## 1. 当前现状

本切片迁移会话状态权威、命令返回边界和配置默认旗标。虽然没有计划中的视觉改版，但任何快照、事件、导航或存档兼容漂移都会直接改变 UI，因此必须用当前源码重建发布闭包并执行真实浏览器人工验收。

构建来源：基线 commit `50c9c686a3196c19b4f08e83ce775e065066054f` 上的 Slice 5 待提交工作树。

`npm run android:sync` 结果：18 个产品文件，15 个直接复制、3 个 Shipping 清洗转换；www 与 Android 各 18 个产品文件，2 个平台文件，unexpected=0，findings=0。

## 2. 验收范围

真实 Microsoft Edge 加载 `http://127.0.0.1:5187/www/`，两个移动视口均执行 `interaction-contract` 20 步：

1. 开场与出身选择；
2. 出身结果与标题入口；
3. 人物状态、挂机确认、任务列表；
4. 池边打鱼连续两次结算及可见累计进度；
5. 章节入口与石路地图；
6. 地图方向控件和同方向出口堆叠；
7. 大门房间、老管家选择、长对话、可切磋状态；
8. Combat 进入、时间轴采样、自动结算返回地图；
9. 实体替换后动作与反馈。

## 3. 自动观测结果

| 视口 | 轮次 | 步数 | failures | 页面 console problems | 终态 |
|---|---|---:|---:|---:|---|
| 540×960 | 首轮 | 20 | 0 | 0 | `STATE_FS_008_MAP_EXPLORE` |
| 390×844 | 首轮 | 20 | 0 | 0 | `STATE_FS_008_MAP_EXPLORE` |
| 390×844 | 异常帧复核重跑 | 20 | 0 | 0 | `STATE_FS_008_MAP_EXPLORE` |

两轮正式证据目录：

- `outputs/arch001_slice5_chapter_session_20260722_540x960`；
- `outputs/arch001_slice5_chapter_session_finalreview_20260722_390x844`。

输出目录不进入 Git。

## 4. 人工逐图检查

首轮 40 张原始截图全部逐张打开检查；390×844 复跑后又人工打开章节入口和两张时间敏感 Combat 图。检查结论：

- 中文标题、按钮、任务、房间、NPC 和反馈可读；
- 顶部“放置江湖”保持单行，关键导航没有隐藏或截断；
- 关键按钮在视口内，没有横向溢出；
- 地图出口没有重叠，大门上下两个同名石路方向可区分；
- NPC 选择、长对话、可切磋和替换后状态可区分；
- 长对话区域可滚动，顶部导航和实体面板仍可用；
- Combat 双方 HP/MP、动作、伤害和日志可读；
- 没有把内部 action/result/entity ID 当作主要玩家文案；
- 未发现 `undefined`、`null`、问号占位、Nova Lite 或射击项目残留文案。

390×844 人物状态页属性标签仍有既有紧凑换行，但值和含义可读；它属于 G5 UI 重构范围，不是本切片新增回归。

## 5. 视觉漂移复核

与 Slice 4 已人工验收的对应证据逐文件比较：

- 540×960：18/20 张 SHA-256 完全一致；2 张变化均为 Combat 时间轴采样点；
- 390×844 正式复跑：18/20 张 SHA-256 完全一致；2 张变化均为 Combat 时间轴采样点；
- 两个视口变化的 Combat 图均以原始分辨率人工打开并通过。

390×844 首轮的第 10 张章节入口捕获到瞬时过渡帧：顶部左导航短暂出现在页面底部。该图未直接判为通过，而是完整重跑 20 步。复跑第 10 张与 Slice 4 已验收图逐字节一致，且 20 步仍为 0 failures / 0 console problems，因此判定为截图时序瞬态，不作为产品稳定缺陷关闭；首轮证据继续保留，不删除。

## 6. ChapterSession 行为证据

浏览器链路证明旧 `createFirstSessionRuntime` facade 在真实 DOM 调用下继续工作：

- 状态、旗标、玩家、任务、房间、实体和事件能跨 20 步连续推进；
- 两次挂机奖励累计为 2 次和经验 401；
- NPC 交谈后的实体替换可见；
- Combat 兼容链结算后回到地图；
- 页面控制台没有模块加载、Schema、快照或事件异常。

## 7. 与生产标准的关系

本证据只证明 Slice 5 当时的 Web 发布闭包在两个目标移动视口没有引入可见回归。该历史切片单独不等于：

- 当时的 `ARCH-001` 完成；Slice 6 UI adapter 已在后续施工完成；
- `T05-01` 11 屏×3 尺寸共 33 对完整验收；
- Android 真机 WebView、性能、内存和离线恢复验收；
- 358/358 动作证明、SAVE-001、OBS-001；
- 签名 Release AAB/APK、商店、隐私、监控或分阶段发布完成。

## 8. 风险

- 本轮在桌面 Edge 移动仿真视口验收，不是 Android WebView 性能结论；
- 章节快照约为大对象，已消除公共命令对整份快照的二次深拷贝，但仍需后续真机预算；
- 当前 Combat 仅为延期兼容时间轴，不是真实 CombatSession；
- 截图证据是生成物，不提交 Git，复现依赖命令、源码 commit 和浏览器环境。

## 9. 未完成项

该历史切片结束时，下一架构切片固定为 UI view-model / intent mapper / browser automation seam；该 Slice 6 已在后续施工和全部回归后完成，当前 `ARCH-001` 已关闭。

`COMBAT-002`、Rest/Repair 和真实 CombatSession 继续延期。G4、G5、G7 以及整个项目上线仍未完成。
