# 本轮推进报告：runtime 执行器 P2 收口

生成时间：2026-07-08

## 本轮目标

沿着上一轮 P2 closure gate 的结论，优先处理会直接影响第一章任务逻辑的 runtime 缺口：

1. 时间/定时标记执行器
2. 技能经验执行器
3. 副本故事对白执行器

本轮不声明 UI 达到上线品质，也不声明从启动到第一章结束已完整复刻。这里只收 runtime result token 的执行闭环。

## 已完成

### 1. 时间/定时标记 executor

修改：

- `H:\MyProjectBack\idlewuxia\src\wuxiaFirstSessionFlow.js`

新增 runtime player state：

- `player.timeMarkers`
- `player.timedMarkers`

新增 result 执行：

- `玩家时间标记设置`
- `玩家定时标记设置`

新增 condition 读取：

- `玩家时间标记等于/大于/小于`
- `玩家定时标记等于/大于/小于`

结果：

- 17 条原 `not_executed_time_marker` 已降级为 `implemented_time_marker`

### 2. 技能经验 executor

修改：

- `H:\MyProjectBack\idlewuxia\src\wuxiaFirstSessionFlow.js`

新增 runtime player state：

- `player.skillExp`
- `player.skillLevels`

新增 result 执行：

- `玩家武功经验变化`

新增 condition 读取：

- `玩家武功等级大于/小于/等于`
- `玩家武功等级大于/小于/等于玩家等级`
- `玩家属性大于/小于/等于`

同时修复了同动作多分支选择问题：现在 NPC / 物件同一个动作下会按配置条件选择第一条满足的分支，而不是固定拿第一条。

结果：

- 9 条原 `not_executed_skill_progression` 已降级为 `implemented_skill_exp_delta`

### 3. 副本故事对白 executor

修改：

- `H:\MyProjectBack\idlewuxia\src\wuxiaFirstSessionFlow.js`

新增 result 执行：

- `副本故事`

行为：

- 从 `Arg2` 读取分号分隔的对白
- 进入统一 `feedbackLines` 管线
- 保留 `Arg3` 分号分隔的行时长字段到 `lineDurations`

结果：

- 3 条原故事对白 P2 已降级为 `implemented_story_dialogue_feedback`

## 审计脚本同步

修改：

- `H:\MyProjectBack\idlewuxia\tools\audit-wuxia-fb01-result-token-runtime-coverage.mjs`

新增 runtime status：

- `implemented_time_marker`
- `implemented_skill_exp_delta`
- `implemented_story_dialogue_feedback`

新增 binding：

- `runtime_player_time_marker_executor`
- `runtime_skill_exp_executor`
- `runtime_story_dialogue_feedback`

## 回归测试

修改：

- `H:\MyProjectBack\idlewuxia\tools\test-wuxia-first-session-interactions.mjs`

新增断言：

- `skillexp11` 写入 `player.skillExp.yiqigong = 15`
- `quest1000` 写入 `player.timeMarkers["送信任务"] = "1"`
- `bftimebj1` 写入 `player.timedMarkers["拜访任务102"].value = "0"`
- `tmstory01` 进入 `feedbackLines`，且对白包含“唐竹”

## 验证结果

已执行：

```powershell
node --check src\wuxiaFirstSessionFlow.js
node --check tools\audit-wuxia-fb01-result-token-runtime-coverage.mjs
node --check tools\test-wuxia-first-session-interactions.mjs
npm.cmd run wuxia:test:first-session-interactions
npm.cmd run wuxia:audit:fb01-result-tokens
npm.cmd run wuxia:audit:p2-closure
npm.cmd run wuxia:check:fast
```

全部通过。

最新 fast gate 日志：

- `H:\MyProjectBack\idlewuxia\outputs\wuxia_check_fast_20260708_executor_closure.log`

## 最新 P2 数量

result token runtime coverage：

- 总行数：316
- P3：307
- P2：9

已实现项：

- `implemented_time_marker`：17
- `implemented_skill_exp_delta`：9
- `implemented_story_dialogue_feedback`：3

P2 closure gate：

- result token P2：9
- flow parity P2：8
- total P2：17
- implementRuntimeRows：0
- unclassifiedRows：0

## 剩余 P2 是什么

### 1. 政绩副作用：4 条

剩余 result：

- `bf1zhengji1`
- `bfzhengji1`
- `zhengji700` x2

当前证据：

- 结构化配置确认它们是 `改变政绩`
- `chapter1_action_router_gate_bridge_20260628` 里已标为 `ModifyReputationOrMerit`
- 旧证据状态是 `pending_runtime_test`
- 当前 result args 为空，不能直接推导数值

结论：

不能把它们猜成 `player.merit += 1` 或 `+=700`。下一步必须追 dispatcher 或运行时测试，否则只能保留为系统进度债务。

### 2. 季节/活动委托：5 条

剩余 result：

- `bainian`
- `bainianweituo`

当前策略：

- 保留 `defer_with_guard`
- 不允许在第一章主线验收里冒充已实现

### 3. flow parity：8 条

剩余：

- 7 条项目导航桥接
- 1 条隐藏项目桥接节点

当前策略：

- 允许保留，但必须数据驱动、标记为项目策略，并且真实浏览器验收不能跳错界面。

## 下一步

1. 追 `改变政绩` 的真实 dispatcher 或运行时行为，产出证据表。
2. 若证据确认数值，接 `player.meritLedger` 或同等数据结构；若仍无法确认，则从第一章可见流程隐藏相关分支。
3. 开始做真实浏览器验收粒度升级：返回链路、NPC 交互、任务点击、地图节点跳转必须逐个截图/断言。

