# 本轮推进报告：Skill 瀑布流真实验收 20260710

## 本轮目标

按 skill 瀑布流继续推进、审计、验收和复盘。验收标准不再只看离线截图或旧脚本结果，而是以当前本地服务 `http://127.0.0.1:5187/` 的真实浏览器流程、配置驱动审计、逐界面/逐交互/逐资产矩阵为准。

## 使用的 Skill 流

1. `game-development-orchestrator`
2. `game-system-architect`
3. `data-configuration-engineer`
4. `level-content-designer`
5. `ui-ux-feedback-designer`
6. `qa-bot-validation-engineer`
7. `wuxia-project-automation`
8. `wuxia-game-planning`
9. `wuxia-system-design`

## 本轮代码改动

1. `tools/run-wuxia-real-browser-flow.mjs`
   - 修复 `all-key-screens` 仍点击旧 `data-wuxia-node-id="NODE_FB01_MAIN_HALL"` 的问题。
   - 支持在没有玩家可见按钮时，通过 `window.__idleWuxiaAutomation.dispatchAction` 记录为 automation fallback。

2. `src/wuxia-main.js`
   - 新增 `window.__idleWuxiaAutomation`，只暴露通用运行时能力：`dispatchAction`、`selectNode`、`selectRoom`、`snapshot`。
   - 不加入任何具体章节、NPC、技能、门派或奖励硬编码。

3. `tools/build-wuxia-skill-waterfall-stage-tasks.mjs`
   - 不再写死读取 20260708 的旧真实浏览器摘要。
   - 自动扫描 `outputs/skill_waterfall_acceptance_*`，按 scenario 选最新 summary。
   - 排除无 scenario 的旧摘要，避免旧结果污染当前门禁。
   - 新增 `realBrowserAutomationDispatches` 指标。
   - 新增 P1 任务：`P1_PLAYER_VISIBLE_KEY_SCREEN_ENTRY`。
   - 新增门禁：`GATE_PLAYER_VISIBLE_KEY_SCREEN_ENTRY`。

## 验收结果

### 真实浏览器 all-key-screens

命令：

```bash
node tools/run-wuxia-real-browser-flow.mjs --scenario all-key-screens --out-dir outputs/skill_waterfall_acceptance_20260710_all_key_screens_after_automation_bridge
```

结果：

- steps: 12
- failures: 0
- finalState: `STATE_FS_011_CHAPTER_LOOP_RETURN`
- 输出：`outputs/skill_waterfall_acceptance_20260710_all_key_screens_after_automation_bridge/real_browser_flow_summary.json`

注意：该结果包含 1 次 automation fallback，说明当前关键状态屏已经能被配置状态机覆盖，但还没有完全转成玩家可见入口。

### Skill 瀑布流任务聚合

命令：

```bash
npm.cmd run wuxia:tasks:skill-waterfall
```

结果：

- taskCount: 12
- P0: 2
- P1: 5
- P2: 3
- P3: 2
- done: 5
- open: 6
- ready_for_next: 1
- gate pass: 5
- gate fail: 2
- realBrowserScenarios: `all-key-screens:12 | chapter1-deep:48`
- realBrowserFailures: 0
- realBrowserBadMatches: 0
- realBrowserAutomationDispatches: 1

输出目录：

- `outputs/skill_waterfall_stage_tasks_20260708/task_list.csv`
- `outputs/skill_waterfall_stage_tasks_20260708/stage_gate_matrix.csv`
- `outputs/skill_waterfall_stage_tasks_20260708/screen_acceptance_matrix.csv`
- `outputs/skill_waterfall_stage_tasks_20260708/interaction_acceptance_matrix.csv`
- `outputs/skill_waterfall_stage_tasks_20260708/asset_acceptance_matrix.csv`
- `outputs/skill_waterfall_stage_tasks_20260708/code_surface_acceptance_matrix.csv`
- `outputs/skill_waterfall_stage_tasks_20260708/stage_task_report.md`

### 严格上线门禁

命令：

```bash
npm.cmd run wuxia:gate:skill-waterfall
```

结果：失败，符合预期。

失败原因：

1. `GATE_PLAYER_VISIBLE_KEY_SCREEN_ENTRY`
   - `automationDispatches=1`
   - 说明还有关键状态屏通过自动化派发到达，不是完整玩家可见入口。

2. `GATE_PRODUCT_VISUAL_PARITY`
   - 当前审计只证明了映射、配置和基础可读性，不证明产品级视觉与竞品界面逐项一致。

### Fast Check

命令：

```bash
npm.cmd run wuxia:check:fast
```

结果：通过。

关键数据：

- first-session states: 11
- actions: 32
- chapter1 rooms: 45
- chapter1 NPCs: 116
- interactables: 23
- interaction actions: 358
- result token rows: 316
- result token P2 rows: 0
- flow parity P2 rows: 0
- content boundary high: 0
- content boundary passStrict: true

## 当前任务清单

### P1_PLAYER_VISIBLE_KEY_SCREEN_ENTRY

状态：open

问题：`all-key-screens` 中还有 1 个关键状态屏靠 automation fallback 进入。

下一步：把 fb01 房间链路与高层状态屏统一，让战斗、NPC、章节回环由玩家可见且配置驱动的房间/NPC/action 触发。

验收：`real_browser_flow_summary.json` 中所有关键屏跳转 `click.automation=false`。

### P1_REAL_BROWSER_FULL_INTERACTION_CRAWL

状态：open

问题：配置里有 358 个 NPC/物件交互动作，真实浏览器现在只跑 curated route。

下一步：做有界浏览器 crawler，每个 fb01 NPC/interactable action 至少点击一次，记录反馈、日志、截图和不可达原因。

### P1_PRODUCT_UI_PARITY_MATRIX

状态：open

问题：当前矩阵只证明无 debug token、无旧项目残留，不能证明界面达到竞品逐屏一致和上线视觉标准。

下一步：为 11 个首会话屏建立竞品源码/配置/录屏截图/目标布局/实际截图的逐屏矩阵。

### P1_ART_ASSET_OWNERSHIP_AND_STYLE

状态：open

问题：现有玩家可见资源仍以占位/生成 UI 为主，不是可商用美术包。

下一步：建立 9:16 武侠 UI kit、背景、角色剪影、图标替换计划；竞品资源只保留为参考证据。

### P2_SERVER_LOGIN_SYNC_PURCHASE_CONTRACT

状态：open

下一步：定义登录、存档同步、奖励校验、购买校验的 JSON 请求响应和伪造拒绝规则。

### P2_OUTPUT_CACHE_HYGIENE

状态：open

下一步：增加安全 cleanup dry-run，只清理 outputs 下浏览器 profile/cache，不碰源码、配置、截图、报告。

### P2_NEXT_CHAPTER_CONFIG_IMPORTER

状态：ready_for_next

下一步：把第二章导入也做成 `chapterN` 配置包，新增系统走 resultEffectPolicies 注册。

## 复盘结论

本轮把“旧验收脚本点不存在节点”的问题收口了，也让 skill 瀑布流任务清单从旧摘要切到当前最新真实浏览器证据。但项目还没有达到上线标准，原因非常明确：配置运行时已经稳定，第一章真实路径和 P2 语义闭环已过；现在卡的是玩家可见交互覆盖、竞品逐屏 UI 还原、产品级资产与视觉标准。

下一轮不应该继续扩系统，也不应该继续做泛泛 UI 美化。最该做的是把 `automationDispatches=1` 清零，然后做 358 个交互的有界真实浏览器 crawler，同时建立 11 屏 UI parity matrix。
