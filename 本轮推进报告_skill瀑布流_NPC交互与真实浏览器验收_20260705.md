# Skill 瀑布流推进报告：NPC 交互与真实浏览器验收

生成时间：2026-07-05

## 本轮原则

- 以放置江湖代码/配置导出的结构化证据为准。
- 录屏只用于界面布局参考，不作为缺失逻辑的替代来源。
- 产品 UI 不允许显示 raw ID、调试字段、审计字段或旧 Nova 模板内容。
- 验收使用真实浏览器 `http://127.0.0.1:5187/`，离线截图只作辅助。

## 已完成

1. 修复入口与构建输出
   - 根入口 `index.html` 已重建为新武侠入口。
   - `npm run build:web` 已刷新 `www`，避免旧 `src/main.js` / Nova Lite 离线弹窗继续污染验收。
   - `wuxia:validate:active-entry` 当前通过，活动入口为 `src/wuxia-main.js`。

2. 接通房间 - NPC - 动作 - 日志链路
   - `fb01` 房间链同步：45 个房间。
   - NPC 同步：116 个 NPC。
   - NPC 动作：318 个。
   - NPC 叙事分支：64 个。
   - UI 已能在房间里选择 NPC，并显示配置驱动的动作按钮。
   - 点击 `武馆老管家 -> 交谈` 后，底部日志显示 `chapter1_results.csv` 解析出的竞品文本。

3. 证据与验收表扩展
   - `outputs/wuxia_first_session_evidence_map/first_session_competitor_evidence_map.csv`
   - `outputs/wuxia_first_session_acceptance/npc_interactions.csv`
   - 新增证据域：
     - `chapter1_npc`: 116
     - `chapter1_npc_action`: 318
     - `chapter1_npc_branch`: 109

4. 真实浏览器验收
   - 初始页：`UI_OpeningStory / STATE_FS_001_OPENING_STORY`
   - 第一章默认房间：`石路`
   - 点击出口进入：`大门`
   - 大门 NPC：`武馆老管家`
   - NPC 动作：`交谈 / 切磋 / 送礼`
   - 交谈日志包含：`金牛武馆比武招贤`
   - 页面检查：
     - 旧模板词 `Welcome Back / GALAXY / TURRET / tesla / Offline Cap`: false
     - raw ID `STATE_FS_ / ACTION_FS_ / NODE_FB01_ / GATE_ / ENC_ / REWARD_ / bfitem / rlt_ / text27`: false
     - 大门中心牌与上下出口重叠：false

真实浏览器截图：

- `H:\MyProjectBack\idlewuxia\outputs\skill_waterfall_acceptance_20260705\real_browser_fb01_gate_steward_interaction_no_overlap_20260705c.png`

## 自动审计结果

命令：

```powershell
npm run wuxia:check:fast
```

结果：

- `validate`: 通过
- `wuxia:validate:active-entry`: 通过
- `wuxia:validate:first-session`: errors 0, warnings 1
- `wuxia:acceptance:first-session`: issues 0
- `wuxia:evidence:first-session`: total 678
- `wuxia:simulate:first-session`: actions 13, rejected 0, expectedRejected 1
- `wuxia:test:first-session-interactions`: passed true
- `wuxia:audit:flow-parity`: P0 0, P1 1, P2 7

## 剩余差异

P1：

- `NODE_FB01_SETTLEMENT_LOOP` 仍未完全竞品确认，需要继续追 `mapRoom / mapRole / mapRoleBase` 证据，或降级为我方压缩表现节点。

P2：

- 7 条返回/离开/状态类 Action 是我方为了原型可操作补的项目策略动作，不应标成竞品事实。

## 下一阶段任务

1. 继续从 `fb01` 原始配置补完非 NPC 交互物，例如 `bfitem*`，不能隐藏也不能显示 raw ID。
2. 把 NPC 的非叙事结果分支接入动作分发，例如 `change* / mapbj* / item* / exp* / delete / stop / inattack*`。
3. 继续逐房间真实浏览器验收：大门、前院、大厅、长廊、书房、馆主卧室、后院。
4. 重新审计 UI 是否仍有临时原型痕迹；当前已可交互，但仍不是上线美术级。
