# 本轮推进报告：Skill 瀑布流架构与真实交互

日期：2026-07-10

## 结论

本轮完成了配置驱动战斗入口的语义修复、真实浏览器关键路线验收、项目继续开发架构包和新增内容审计。当前状态是 `framework_ready_product_blocked`，不是上线完成。

严格门禁为 6 通过、2 失败。失败项是 `GATE_PRODUCT_VISUAL_PARITY` 与 `GATE_COMBAT_PRODUCT_PRESENTATION`；逐屏视觉对照、自有资产、战斗表现与上线级艺术质量仍未完成。

## 竞品语义修复

竞品 Lua 证据：

- `FightResult.lua:436-517`：主动切磋先进入战斗，战斗结束后按成功、失败、逃跑执行条件结果。
- `BaseMap.lua:3235-3500`：`afterFightWithQieCuo` 在战斗结束回调后恢复地图并派发结果。
- `ConditionMap.lua:713-725`：切磋失败和逃跑是独立结果。
- `fb01r01_1 / fb01r01_1a`：交谈换人后才能切磋，`comparewin` 后才执行 `text28/mapbj2/change11`。

修复内容：

- `comparewin` 不再脱离战斗结果上下文自动成立。
- 新增配置 `chapterSystem.combatActionPolicies.compete`。
- 运行时新增通用 `pendingCombat` 与 `resolvePendingCombat`，没有写死老管家 ID。
- 固定预览的胜利结果由配置动作声明，结果分支在战斗完成动作后执行。
- 交互回归由旧的即时成功路径改成 `compete -> combat -> comparewin -> return map`。

## 真实验收

自动真实浏览器：

- 产物：`outputs/skill_waterfall_acceptance_20260710_all_key_screens_visible_compete_v2`
- 540x960
- 16 个玩家可见步骤
- 0 失败
- 0 automation dispatch
- 最终状态：`STATE_FS_011_CHAPTER_LOOP_RETURN`

应用内浏览器人工点击链：

- 15 次可见点击全部通过。
- 覆盖出身、标题、状态、挂机任务、奖励领取、章节入口、房间、NPC 选择、交谈、切磋、战斗结果与章节循环。
- 最终界面：`UI_ChapterLoop`。

运行时与数据审计：

- `npm run wuxia:check:fast`：通过。
- 交互回归：通过，55 个事件。
- 第一章配置：45 房间、116 NPC、23 可交互物、358 个交互动作。
- 结果语义：316 行，P0/P1/P2 为 0；P3 仍保留在审计中。
- 活跃代码内容硬编码：high=0，strict pass。

严格上线门禁：

- `npm run wuxia:gate:skill-waterfall`：退出码 1。
- 原因：`GATE_PRODUCT_VISUAL_PARITY` 与 `GATE_COMBAT_PRODUCT_PRESENTATION` 仍失败。
- 此失败必须保留，不能用框架通过或离线截图替代。

## 架构与规范产物

目录：`outputs/wuxia_development_framework_package_20260710`

- `architecture_framework.md`
- `current_completion_audit.csv`
- `module_architecture.csv`
- `configuration_standard.csv`
- `interaction_standard.csv`
- `screen_experience_standard.csv`
- `art_asset_standard.csv`
- `competitor_evidence_line_audit.csv`
- `development_task_list.csv`
- `new_content_line_audit.csv`
- `summary.json`

根目录新增 `CONTEXT.md`，固化 Room、Presentation Node、Interaction Action、Condition Token、Result Token、Combat Session、Action Route、Owned Asset 等领域术语。

## 下一阶段任务

1. `P1_FULL_INTERACTION_BROWSER_CRAWL`：把真实浏览器覆盖从关键路线扩到 358 个交互动作及不可达/锁定原因。
2. `P1_SCREEN_PARITY_AND_RESPONSIVE_QA`：11 个首局界面逐屏做竞品证据、目标截图、实际截图和 360x640/540x960/720x1280 响应式门禁。
3. `P1_OWNED_ART_AND_MOTION_KIT`：建立自有 UI、背景、角色、战斗、FX、音频资产组和替换清单。
4. `P1_COMBAT_PRESENTATION_PRODUCT_PASS`：把固定预览升级为单位绑定状态、胜负/逃跑分支、Buff、打击反馈和表现节奏。
5. `P2_SERVER_ADAPTER_AND_PURCHASE_VERIFY`：实现登录、同步、奖励领取、购买校验的内存与 HTTP adapter。
6. `P2_CONFIG_SCHEMA_VERSIONING`：拆分当前超大首局 JSON，并加入版本、引用和迁移校验。
7. `P2_OUTPUT_AND_LEGACY_CONFIG_HYGIENE`：治理旧 prototype 配置和输出缓存，保留源码、证据和当前 Wuxia 入口。
8. `P2_NEXT_CHAPTER_IMPORTER`：用同一配置契约导入下一章，验证运行时代码无需新增具体内容 ID。

## Skill 使用结果

- 策划/系统：证据等级、章节配置、Combat Session 与 ServerGateway 责任已固化。
- 代码架构/领域建模：形成 12 个深模块接口和根领域词汇。
- TDD/诊断：真实故障先复现为红测试，再修复到单测和真实点击双绿。
- UX/UI/包装/美术：形成逐屏体验规范和 7 类资产组；因为当前实际界面仍粗糙，视觉门禁保持失败。
- 自动化/测试：快检、真实浏览器、严格门禁和新增内容逐文件审计均已产出。
- UE/Godot 通用规则只用于数据驱动和真实运行验收口径；本轮没有把 Web 主原型改成其他引擎。
