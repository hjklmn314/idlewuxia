# 首局流程主项目验收 2026-07-03

## 范围

主项目：`H:\MyProjectBack\idlewuxia`

本轮只收口用户明确指出的三个高优先问题：

1. 不是从启动/开场开始，而是从状态页开始。
2. `prototype-novalite` 的英文离线收益弹窗覆盖武侠流程。
3. 任务列表点击“池边打鱼”没有反馈。

## 已同步到主项目

- `src/main.js`
- `src/wuxiaFirstSessionFlow.js`
- `src/styles.css`
- `config/wuxia_first_session_flow.json`
- `config/wuxia_first_session_screen_contract.json`
- `tools/migrate-wuxia-first-session-v2.mjs`
- `tools/test-wuxia-first-session-interactions.mjs`
- `tools/simulate-wuxia-first-session-flow.mjs`
- `package.json`

## 自动验证

命令：

```powershell
cd /d H:\MyProjectBack\idlewuxia
npm.cmd run wuxia:check:fast
```

结果：

- `validate-config`: 通过，findings 为空。
- `wuxia:validate:first-session`: `states=12`、`actions=19`、`chapter1Nodes=7`、`errors=0`、`warnings=0`。
- `wuxia:simulate:first-session`: `actions=8`、`rejected=0`、`expectedRejected=1`。
- `wuxia:test:first-session-interactions`: `passed=true`、`finalState=STATE_FS_006_SECT_HOME`、`finalExperience=201`、`finalPotential=201`、`events=8`。

## 浏览器真实验收

URL：

```text
http://127.0.0.1:5177/?t=first-session-fix-20260703b
```

验收步骤：

1. 刷新页面。
2. 首屏确认出现：
   - “某年某日，”
   - “一个宁静的小镇，”
   - “有一位无名少女诞生了。”
   - “这位无名少女出生在：”
   - “武学世家 / 书香门第 / 商贾之家 / 孤儿”
3. 点击 `书香门第`。
4. 点击 `继续`。
5. 点击 `点击开始游戏`。
6. 点击状态页任务条。
7. 点击 `开始任务`。
8. 点击 `池边打鱼`。

浏览器验收结论：

- 首屏为开场叙事，不再从状态页开始。
- 没有 `OFFLINE / Welcome Back / WATCH AD` 弹窗。
- `池边打鱼` 点击后显示“你完成了一次池边打鱼，获得经验 200、潜能 200。”
- 当前任务列表暴露 4 个配置 action：
  - `ACTION_FS_005_IDLE_TASK_CLICK_POOL_FISH`
  - `ACTION_FS_005_LOCKED_EXP_1000`
  - `ACTION_FS_005_LOCKED_EXP_2000`
  - `ACTION_FS_005_IDLE_TASK_LIST_CONTINUE`

## 仍需下一步清理

1. `prototype-novalite` 的隐藏 DOM 仍在页面结构中，虽然已不显示、不再弹离线收益；后续要做武侠主壳剥离。
2. 其他出身分支目前只有按钮标签来自录屏，结果文案标为 `recording_observed_label_only`，不能当竞品事实。
3. 当前任务点击反馈已经有，但还没有完整复刻竞品后续挂机倒计时、长时间收益、回到状态页后的数值展示链。
