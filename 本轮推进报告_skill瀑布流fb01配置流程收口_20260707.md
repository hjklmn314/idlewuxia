# Skill 瀑布流 fb01 配置流程收口报告 20260707

## 使用的 Skill

- `wuxia-game-planning`：按证据等级区分竞品事实、反推、项目实现，不把未知当完成。
- `wuxia-ux-ui-design`：验收点击反馈、房间/NPC/物件交互、移动拦截是否给玩家可见反馈。
- `wuxia-project-automation`：用脚本化检查和真实浏览器 540x960 截图验收，而不是只看静态截图。

## 本轮已改

1. `src/wuxiaFirstSessionFlow.js`
   - 接入 result side-effect 执行器：换人、添加人物、删除人物/自身、地图标记、玩家属性、普通物品变化、玩家标记。
   - 接入 `goromeX + stop` 房间移动拦截：未打赢赵教头/周教头时，进入 `fb01_03` 会被对应 NPC 拦住并显示竞品文本。
   - 修正 `change*` 为“原位置替换”，不是追加到房间列表末尾，避免赵/周教头顺序错误。
   - 接入有完整参数的 `物品合成` recipe：扣原料、加产物、记录合成文本。
   - 对 `compare/inattack*` 战斗 result 做显式记录，但不伪装为完整战斗结算。
   - `bainian/bainianweituo` 标为春节活动委托记录项，不再误判为第一章主线阻断。

2. `src/wuxia-main.js`
   - 房间人物/物件渲染改为读取 runtime 快照里的 `hiddenEntityIds`、`dynamicEntityIdsByRoom`、`replacementEntityById`。
   - `roomBlocked` 事件会刷新 UI，并在房间日志显示“赵教头拦住了你 / 周教头拦住了你”。

3. `tools/sync-fb01-result-runtime-args.mjs`
   - 从 `fangzhijianghu/outputs/fzjh_chapter1_planning_audit_20260622/chapter1_results.csv` 回灌 `Arg2..Arg7` 到 `config/wuxia_first_session_flow.json`。
   - 本轮同步：316 条 resolved result，316 条更新，0 缺失。

4. `tools/audit-wuxia-fb01-result-token-runtime-coverage.mjs`
   - 重写 result token runtime 覆盖分类。
   - 不再把已接入的换人/地图标记/属性/物品/移动拦截误报为未执行。
   - `bainian/bainianweituo` 归入 `not_bound_seasonal_activity_delegate`，保留证据缺口但不阻断第一章主线。

5. `tools/test-wuxia-first-session-interactions.mjs`
   - 新增赵/周教头门槛链路测试：
     `fb01r02_1b talk -> change16 -> fb01r02_1 -> 被赵拦 -> compete -> 被周拦 -> compete -> 进入 fb01_03`。

6. `tools/run-wuxia-real-browser-flow.mjs`
   - `chapter1-deep` 真实浏览器验收脚本改为按竞品配置流程走赵/周教头拦截与切磋解锁，不再跳过门槛。

7. `package.json`
   - 新增 `wuxia:sync:fb01-result-args`，并接入 `wuxia:check:fast`。

## 验收结果

- `npm.cmd run wuxia:check:fast`：通过，退出码 0。
- `npm.cmd run wuxia:test:first-session-interactions`：通过，37 个事件。
- `npm.cmd run wuxia:audit:fb01-result-tokens`：
  - 总 result 行：316
  - P0：0
  - P1：3
  - P2：38
  - P3：275
- 真实浏览器验收：
  - 命令：`node tools/run-wuxia-real-browser-flow.mjs --scenario chapter1-deep --out-dir outputs/skill_waterfall_acceptance_20260707_chapter1_deep_after_recipe_fix`
  - 视口：540x960
  - 步数：46
  - failures：0
  - 截图目录：`outputs/skill_waterfall_acceptance_20260707_chapter1_deep_after_recipe_fix`

关键截图：

- `18_blocked_by_zhao_before_compete.png`：进入内院前被赵教头拦住。
- `20_coach_zhao_compete_win.png`：赵教头切磋胜利后显示“可以进去了”。
- `21_blocked_by_zhou_before_compete.png`：赵通过后仍被周教头拦住。
- `23_coach_zhou_compete_win.png`：周教头切磋胜利后解锁。

## 当前仍未达上线标准的地方

这次只收口了第一章主流程配置执行，不代表视觉和完整系统已上线。

P1 未完成：

- `combat_trigger_recorded_but_no_battle_resolution`：`compare`、`inattack201`、`inattack202` 已记录触发目标，但还没有接真实战斗/传承战斗结算。

P2 未完成：

- `player_time_marker_executor`：17 条时间/定时标记。
- `skill_exp_executor`：9 条武功经验变化。
- `specific_result_executor`：7 条特殊 result。
- `seasonal_activity_delegate_out_of_first_session_scope`：5 条春节拜年/拜年委托活动逻辑。

UI/表现问题：

- 当前网页仍是原型 UI，不是产品级美术表现。
- 已能按竞品配置完成点击反馈、房间移动、NPC/物件交互，但布局、字体、动效、资产包装还没有达到上线标准。
- 后续 UI 验收必须逐界面对照竞品界面代码/配置和录屏，不允许再用“功能说明面板”替代玩家界面。

## 下一阶段任务

1. 实现 P1 executor：
   - 主动切磋/传承战斗入口。
   - 把战斗结果回写到 `comparewin`、`inattack*` 后续分支。

2. 做第一章 UI 逐屏审计：
   - 启动、出生选择、状态、任务、房间探索、NPC 交互、物件交互、战斗入口。
   - 每屏列竞品配置字段、界面代码证据、录屏参考、当前实现差异。

3. 做资产审计：
   - 背景、底部剪影、按钮、黑白描边字体、房间/人物/物件交互层级。
   - 区分可内部参考的竞品资源和必须重制的商业资源。

4. 扩展真实浏览器验收：
   - 增加 P1 合成/战斗路线。
   - 增加返回键、重复点击、隐藏物件不可点、已换人后旧实体不可点。
