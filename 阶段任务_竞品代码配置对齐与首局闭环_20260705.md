# 阶段任务：竞品代码配置对齐与首局闭环

## 当前结论

1. 当前主入口必须只加载 `src/wuxia-main.js`。旧 `src/main.js` 和 Nova Lite 射击模板可以保留为迁移材料，但不能进入武侠首局运行包。
2. 第一章 7 节点是我方竖屏压缩呈现层，不是竞品原始地图的完整结构。竞品原始 `fb01` 仍需要下钻到 45 个房间、角色、条件结果、奖励和地图 flag。
3. `UI_SectHome` 只能作为录屏布局参考，不能在未选择门派前默认进入华山或任何门派页。
4. 凡是没有 Lua/config 证据的内容，只能标 `design_proposal` 或 `unknown`，不能写成竞品事实。

## 已收口

- 活跃入口已隔离到 `src/wuxia-main.js`。
- 新增 `wuxia:validate:active-entry`，门禁旧模板信号：`Welcome Back`、`Offline`、`GALAXY`、`TURRET`、`Nova Reactor`。
- `wuxia:check:fast` 已纳入活跃入口门禁。
- `UI_SectHome` 已从强制首局顺序中移除，降级为录屏布局参考。
- 流程审计已避免把内部 `STATE_FS_*`、`ACTION_FS_*`、`NODE_FB01_*` 当成玩家可见文本误报。
- 新增 `wuxia:sync:fb01-room-details`，从 `fangzhijianghu/outputs/fb01_room_chain_package_20260629/fb01_room_nodes.csv` 同步房间名、人物名、节点 gate。
- 当前 7 个第一章节点已同步出 12 个 gate、7 个 reward；`REWARD_FB01_OWNER_BRANCH` 和 `REWARD_FB01_TRAINING_ITEMS` 只确认奖励 ID，数值明细仍标 unresolved，不能当 0。
- 点击章节节点时，运行时事件会返回房间、人物、门槛、战斗、奖励、出口和证据；UI 渲染层只读取数据，不写死具体节点内容。
- 新增 `wuxia:sync:hangup-reward-class`，从 `client_runtime_summary.json` 同步 `award_class=100012`，`ACTION_FS_005_IDLE_TASK_CLICK_POOL_FISH` 不再直接写死 `statDeltas`，由 rewardClass 展开经验/潜能。

## 本轮验证

- `npm run wuxia:sync:fb01-room-details`：通过，同步 7 节点，新增 2 条 unresolved 支线奖励。
- `npm run wuxia:sync:hangup-reward-class`：通过，`100012` 展开为 `exp 200`、`pot 200` 两行。
- `npm run wuxia:check:fast`：通过。
- `npm run wuxia:assert:dev-server`：通过，`http://127.0.0.1:5187/` 静态入口无旧 Nova 模板信号。
- 剩余 warning：`NODE_FB01_SETTLEMENT_LOOP` 没有真实 `sourceRoomNames`，因为它是我方压缩结算节点，不是竞品原始房间。

## P0 下一步

1. 把 `fb01_room_runtime_contract.json` 的 45 房间链路接到当前首局 runtime：每个压缩节点可展开房间列表、出口、NPC、可执行动作。
2. 把 7 节点继续下钻成 45 房间浏览/交互层，而不是只显示压缩节点摘要。
3. 把挂机任务 `10001` 的奖励从真实 `HangUpTask` 配置和奖励类解析出来，不再只显示写死的 200/200。
4. 修复任务列表交互：锁定行只显示门槛和未解锁反馈；可执行行才触发任务推进。
5. 继续真实浏览器点击验收：从启动、出身、标题、状态、任务、挂机、章节入口、第一章节点，逐步确认每一步都有反馈和返回路径。

## P1 下一步

1. 拆 `mapConditionAndResult/fb01.lua` 的 `other` 结果语义，生成 `fb01_quest_action_semantics.csv`。
2. 将第一章节点的 UI 文案全部从 screen contract / room contract 读取，渲染层只负责组件化展示。
3. 建立 `first_session_action_route.csv`，列出 `ActionId -> ServerCommand -> ToState -> Evidence`。
4. 将 `runtime_slim` 战斗事件绑定到第一章前厅试手节点，避免节点直接跳假战斗。

## 验收门

- `npm run wuxia:validate:active-entry`
- `npm run wuxia:check:fast`
- 真实浏览器从 `http://127.0.0.1:5187/` 手动点击，不允许用离线截图替代。
- 任何首局可见文本不得出现内部 ID、调试说明、Nova Lite 模板文本、未证实的门派跳转。
