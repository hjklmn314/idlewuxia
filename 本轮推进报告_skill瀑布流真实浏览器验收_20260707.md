# 本轮推进报告：Skill 瀑布流真实浏览器验收 20260707

## 使用的 Skill 口径

- `wuxia-game-planning`：所有竞品结论按证据等级区分，未确认内容不得写成竞品事实。
- `wuxia-ux-ui-design`：首局 UI 必须回答当前状态、可点击动作、输入反馈和返回路径；玩家界面不得出现 debug/raw id/占位符。
- `wuxia-project-automation`：自动审计与真实运行验收并行推进，视觉/交互验收必须基于真实浏览器或真实 PIE，不用离线说明替代。

## 已修复

1. 修复 `src/wuxia-main.js` 的坏模板字符串。
   - 修掉 `{escapeHtml(...)}` 被直接输出到玩家界面的 P0 问题。
   - 修掉多处乱码 fallback 文案导致的语法错误。
   - `node --check src/wuxia-main.js` 已通过。

2. 重建 `index.html` 入口壳层。
   - 唯一入口仍为 `./src/wuxia-main.js`。
   - 清理入口标题、aria、初始 loading 文案乱码。
   - 未恢复旧 Nova Lite / Idle Dot Shooter 模板。

3. 修复首场战斗预览占位问号。
   - `combatPreviews.first_session_old_steward` 的右方单位从 `fb01r01_1` 配置取名为“武馆老管家”。
   - 移除未确认的占位 Buff。
   - 事件、跳字、日志改为可读的配置化预览数据。
   - `UI_EarlyCombat` 的按钮从 `??` 改为 `继续`。

4. 修正在线标准审计脚本。
   - 不再对 `UI_EarlyCombat` 无条件报旧的静态战斗页问题。
   - 改为检查：是否使用 `combatRuntime`、是否存在 combat preview、是否有单位名/事件、是否含问号占位。

5. 新增真实浏览器流程验收脚本。
   - 新增 `tools/run-wuxia-real-browser-flow.mjs`。
   - 使用 Edge CDP 打开真实页面、按数据 ActionId/selector 点击、逐步截图和记录 DOM 摘要。
   - 输出目录：`outputs/skill_waterfall_acceptance_20260707`。

## 验收结果

已运行：

```powershell
node --check src/wuxia-main.js
npm.cmd run wuxia:check:fast
node tools/run-wuxia-real-browser-flow.mjs
```

结果：

- `wuxia:check:fast`：通过。
- `validate:first-session`：11 状态、32 动作、45 房间、12 门槛、7 奖励，0 error / 0 warning。
- `wuxia:test:first-session-interactions`：通过，最终状态 `STATE_FS_011_CHAPTER_LOOP_RETURN`。
- `wuxia:audit:fb01-interactions`：358 个 NPC/物件动作，高风险 0。
- `wuxia:audit:online-standard`：P0/P1 问题 0。
- `run-wuxia-real-browser-flow`：真实浏览器 12 步点击，失败 0，最终状态 `STATE_FS_008_MAP_EXPLORE`。

关键截图：

- `outputs/skill_waterfall_acceptance_20260707/01_opening_story.png`
- `outputs/skill_waterfall_acceptance_20260707/12_old_steward_talk.png`
- 汇总：`outputs/skill_waterfall_acceptance_20260707/real_browser_flow_summary.md`

## 仍未达到“上线级”的地方

这些不能算完成：

1. 当前房间探索 UI 仍是文字化原型。
   - 已能显示房间、出口、NPC、NPC 操作和对白日志。
   - 但视觉层级、背景资产、按钮质感、动效仍没有达到可上线表现。

2. 第一章仍是“45 房间链路 + 7 个压缩节点”的混合呈现。
   - 45 房间数据已在配置中。
   - 7 节点压缩层属于项目桥接，不是竞品原始地图结构。

3. 还有 8 个 P2 流程对齐项。
   - 主要是返回/离开/状态入口等项目导航桥接动作。
   - 它们已标记为 `project proposal`，不能当竞品事实。

4. `fb01r20_1` 的一句默认对白包含疑似源文本异常：
   - 当前源证据为：“老爷一手创办这金牛武馆，姝是不易啊。”
   - 该文本来自 restored `map_role`，本轮不擅自改成“甚是”，后续需设备或更高质量明文复核。

## 下一阶段任务

1. 把真实浏览器验收扩到完整首章主线闭环。
   - 当前自动验收到老管家交谈。
   - 下一步补张风、后院/书房/馆主卧室、隐藏物件、任务结果和章节回环。

2. 把文字化房间 UI 改成接近竞品的产品级竖屏界面。
   - 保留数据驱动：房间、出口、NPC、物件、动作、对白都从 JSON/Lua 派生配置读取。
   - 不允许在 UI 里写死“华山”、任意门派、假奖励或说明性调试文案。

3. 增加源文本异常审计。
   - 扫描 `map_role`、`map_item`、`map_condition_result` 中的疑似乱码/占位符。
   - 分为 `source_confirmed_typo`、`decrypt_suspect`、`needs_device_recheck`。

4. 将 `run-wuxia-real-browser-flow.mjs` 纳入 fast check 的可选人工验收链路。
   - 常规 CI 不强制打开浏览器。
   - 阶段验收必须跑真实浏览器脚本并保留截图。
